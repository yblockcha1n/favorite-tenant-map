-- Admin seeding is handled by the /api/admin/seed route
-- This migration is a placeholder to document the intent
-- The seed route reads ADMIN_EMAIL and ADMIN_PASSWORD from env vars
-- and creates the initial admin user with a bcrypt-hashed password
SELECT 1;
